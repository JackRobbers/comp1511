#include <stdio.h>

struct person {
    int shoe_size;
    double height;
    char first_name_initial;
};

enum opal_card_type {
    ADULT,
    STUDENT,
    CONCESSION
};
int main(void) {
    
    return 0;
}