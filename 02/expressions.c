// print out expressions from tute 02

#include <stdio.h>

int main(void) {
    printf("value of `(7/2)` is 
    printf("value of `(3.0 / 2) + 1`
    printf("value of `'a' + 5`
    printf("value of `'F' - 'A' + 'a'`
    return  0;
}
